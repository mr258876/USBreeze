#include "RGBControl.h"

#if RGB_CUSTOM_LAMP_POSITIONS
const LampPosition RGB_Lamp_Positions[] =
    {
        {0, 0, 0},  // X, Y, Z position in MILLImeters.
        {1, 0, 0},  // X, Y, Z position in MILLImeters.
        {2, 0, 0},  // X, Y, Z position in MILLImeters.
        {3, 0, 0},  // X, Y, Z position in MILLImeters.
        {4, 0, 0},  // X, Y, Z position in MILLImeters.
        {5, 0, 0},  // X, Y, Z position in MILLImeters.
        {6, 0, 0},  // X, Y, Z position in MILLImeters.
        {7, 0, 0},  // X, Y, Z position in MILLImeters.
        {8, 0, 0},  // X, Y, Z position in MILLImeters.
        {9, 0, 0},  // X, Y, Z position in MILLImeters.
        {10, 0, 0}, // X, Y, Z position in MILLImeters.
        {11, 0, 0}, // X, Y, Z position in MILLImeters.
        {12, 0, 0}, // X, Y, Z position in MILLImeters.
        {13, 0, 0}, // X, Y, Z position in MILLImeters.
        {14, 0, 0}, // X, Y, Z position in MILLImeters.
        {15, 0, 0}, // X, Y, Z position in MILLImeters.
};
#endif
